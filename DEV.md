# Test

That would be good.

# Build

Developed in Python 3.12 on Windows.

```shell
pip install -e . --group=dev
```

```shell
python -m pip lock .
```

```shell
pyinstaller main.py -n trf_export --onefile
```