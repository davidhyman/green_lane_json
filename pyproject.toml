[project]
name = "grm_export"
version = "2.2"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp",
    "asynciolimiter",
    "diskcache",
    "enlighten",
    "gpxpy",
    "kml2geojson",
    "mapbox-vector-tile",
    "pgeocode",
    "pydantic",
    "pydantic-settings",
    "pyproj",
    "rdp",
    "shapely",
]

[project.urls]
Homepage = "https://github.com/davidhyman/green_lane_json"

[dependency-groups]
dev = ["pyinstaller", "pip-tools"]
